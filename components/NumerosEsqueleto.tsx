// O fallback. Mesma altura, mesma margem, mesmo lugar — para a linha de baixo
// não se mexer no instante em que o número chega.
export default function NumerosEsqueleto() {
  return <p className="numeros esqueleto" aria-hidden="true" />;
}
